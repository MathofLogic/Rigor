## audit delta — did this change make it worse?

### `resistance` — 🛑 BLOCK (net +11)
**θ-cliff crossings (a symbol became a hub):**

- `validate` blast radius 0 → **11** (cliff 8.0)

> 1 theta-cliff crossing(s): a symbol crossed into STIFF/hub territory

_tier: per-symbol counts are FORCED; the delta budget is STIPULATED, so the composite verdict caps at STIPULATED._

### `arch` — ✅ SHIP (net +0)

_tier: per-symbol counts are FORCED; the delta budget is STIPULATED, so the composite verdict caps at STIPULATED._
